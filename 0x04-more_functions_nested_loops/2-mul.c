#include "main.h"
/**
 * mul - is used to multiply two numbers
 * Return: always 0
 */
int mul(int a, int b);
{
	printf("Enter the values of a and b");
		Scanf("%d%d, %a%b");

		int x = a*b;
		printf("%d", x);
		return (0);
}
