#include "main.h"
#include <stdio.h>
/**
 * main - check code
 * 
 * Return: 1 if it is in uppercase, otherwise 0
 */
int _isupper(int c);
{
	if(c >= 65; && c<= 90)
		return (1);
	 else 
		return (0);
}
